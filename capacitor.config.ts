import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.deliverypwaapp',
  appName: 'Delivery App',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
